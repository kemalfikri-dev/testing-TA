<nav class="navbar navbar-expand-lg fixed-top">
  <div class="container">
    <a class="navbar-brand d-flex align-items-center gap-2" href="/">
      <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="currentColor" class="bi bi-box-seam" viewBox="0 0 16 16">
        <path d="M8.186 1.113a.5.5 0 0 0-.372 0L1.846 3.5l2.404.961L10.404 2zm3.564 1.426L5.596 5 8 5.961 14.154 3.5zm3.25 1.7-6.5 2.6v7.922l6.5-2.6V4.24zM7.5 14.762V6.838L1 4.239v7.923zM2.623 3.836l5.377 2.15 5.377-2.15-5.377-2.15z"/>
      </svg>
      Pesen.In
    </a>

    <button class="navbar-toggler border-0 shadow-none" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto ps-lg-4">
        <li class="nav-item">
          <a class="nav-link" href="/menus">Menus</a>
        </li>
        <% if (user && user.role === 'admin') { %>
        <li class="nav-item">
          <a class="nav-link" href="/categories">Categories</a>
        </li>
        <% } %>
        <li class="nav-item">
          <a class="nav-link" href="/orders">Orders</a>
        </li>
      </ul>

      <ul class="navbar-nav align-items-lg-center gap-2">
        <% if (user) { %>
          <li class="nav-item">
            <span class="nav-link fw-bold text-primary">Halo, <%= user.name %>!</span>
          </li>
          <li class="nav-item">
            <a class="btn btn-secondary btn-sm rounded-pill px-3" href="/auth/logout">Logout</a>
          </li>
        <% } else { %>
          <li class="nav-item">
            <a class="btn btn-primary btn-sm rounded-pill px-4" href="/auth/login">Login</a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>

<!-- Spacer to prevent content from hiding behind fixed navbar -->
<div style="height: 80px;"></div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>