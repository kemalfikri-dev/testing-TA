<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Kategori</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

  <div class="container mt-5">
    <div class="row justify-content-center">
      <div class="col-md-5">
        <div class="card shadow">
          <div class="card-body p-4">
            <h4 class="card-title text-center mb-4">Edit Kategori</h4>

            <!-- Tampilkan error kalau ada -->
            <% if (error) { %>
              <div class="alert alert-danger"><%= error %></div>
            <% } %>

            <form action="/categories/edit/<%= category.categories_id %>" method="POST">
              <div class="mb-3">
                <label>Nama kategori</label>
                <input type="text" name="categories_name" class="form-control" value="<%= category.categories_name %>" required>
              </div>
              <button type="submit" class="btn btn-primary w-100">Submit</button>
            </form>
            <button type="submit" class="btn btn-warning w-100" href="/categories">Kembali</button>
          </div>
        </div>
      </div>
    </div>
  </div>

</body>
</html>